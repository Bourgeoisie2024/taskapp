name: Secrets Demo
on:
  push:
    branches:
      - main
    paths:
      - 'secrets/**' # Trigger the workflow only when files in the secrets directory are changed
jobs: 
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Use secret in deployment
        run: |
          echo "Use secret in deployment"
          echo "Secret value: ${{ secrets.MY_SECRET }}" # Access the secret value using the secrets context
          # Add deployment commands here, e.g., scp, rsync, or cloud deployment commands that utilize the secret


